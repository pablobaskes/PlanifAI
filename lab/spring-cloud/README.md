# Spring Cloud Lab

This folder preserves the previous PlanifAI Spring Cloud experiment:

- `config-server`
- `eureka-server`

These services are not part of the active PlanifAI runtime. They are intentionally excluded from the root Maven reactor and from Docker Compose.

See:

- `docs/adr-001-simplify-spring-cloud-runtime.md`
- `docs/spring-cloud-lab-lessons.md`
